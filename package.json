{
  "name": "edriffles-blog",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run build:astro",
    "build:astro": "astro build",
    "build:production": "npm run build:astro && npm run sequoia:publish && npm run sequoia:inject",
    "build:cloudflare": "node scripts/build-cloudflare.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "sequoia:publish": "sequoia publish",
    "sequoia:inject": "sequoia inject --output dist",
    "sequoia:sync": "sequoia sync",
    "audit:standard-site": "python3 scripts/audit.py"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.5",
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.2",
    "sharp": "^0.35.0"
  },
  "devDependencies": {
    "sequoia-cli": "^0.5.7"
  }
}
